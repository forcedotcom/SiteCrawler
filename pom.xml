<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.salesforce.webdev</groupId>
    <artifactId>SiteCrawler</artifactId>
    <version>0.0.4-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>SiteCrawler</name>
    <description>This project provides a simple WebCrawler with retry-capabilties, functionality to distinguish between http/https sites.

It biggest feature is that it allows for plugins (or CrawlerActions), which allows you to hook your scripts into the crawling process.

It also allow for setting &quot;blocked&quot; URLs. Those URLs or patterns will not be crawled.</description>
    <inceptionYear>Feb 1, 2014</inceptionYear>
    <organization>
        <name>Salesforce.com</name>
        <url>http://www.salesforce.com</url>
    </organization>
    <scm>
        <url>https://git.soma.salesforce.com/webdev/SiteCrawler</url>
        <developerConnection>Jasper Roel</developerConnection>
    </scm>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>jcl-over-slf4j</artifactId>
            <version>1.7.5</version>
        </dependency>
        <dependency>
            <groupId>net.sourceforge.htmlunit</groupId>
            <artifactId>htmlunit</artifactId>
            <version>2.13</version>
        </dependency>
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <version>2.6</version>
        </dependency>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.8.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.1</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <distributionManagement>
        <repository>
            <uniqueVersion>false</uniqueVersion>
            <id>salesforce_releases</id>
            <url>${nexus.release.url}</url>
        </repository>
        <snapshotRepository>
            <uniqueVersion>true</uniqueVersion>
            <id>salesforce_snapshots</id>
            <url>${nexus.snapshot.url}</url>
        </snapshotRepository>
    </distributionManagement>

    <url>https://git.soma.salesforce.com/webdev/SiteCrawler</url>
</project>
